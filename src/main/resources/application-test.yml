spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring_db_test?useSSL=false
    username: ensitech_M1-DI
    password: PZYPzyMPLMwwEtC1B3st#*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  test:
    database: replace=none

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
